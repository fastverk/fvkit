//! Linux backend (P6): btrfs subvolume / loopback fs for managed
//! volumes, Secret Service for the keychain, a systemd user unit for the
//! daemon, and polkit/pkexec for the rare privileged step. Stub for now
//! so `fvkit` builds on Linux.
